---
const { class: className, id, style } = Astro.props
---

<section
  id={id}
  data-section={id}
  class={`section ${className}  px-4 md:px-40 text-primary`}
  style={style}
  >
  <slot />
</section>